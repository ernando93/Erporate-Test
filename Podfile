# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'ErporateTest' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for ErporateTest
      pod 'Alamofire'
      pod 'SwiftyJSON'
      pod 'SDWebImage/WebP'
      pod 'GoogleMaps'
      pod 'GooglePlaces'
      pod 'Firebase/Auth'

end
